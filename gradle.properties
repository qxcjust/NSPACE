# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# AndroidX
android.useAndroidX=true
android.nonTransitiveRfc=false

# Disable the local Maven repository to keep builds reproducible.
systemProp.org.gradle.internal.publish.never=false
